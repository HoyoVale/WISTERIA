# R1.8 Phase 0B–0D Final Fix 基线（2026-08-09）

> 状态：**COMPLETED（代码复审后）**；0E HOLD 至复审通过。

## 1. 修复清单

```text
1. loopMotion 配置冻结（双向漂移拒绝）
2. RestoreCheckpoint 完整语义校验（in-memory == wire 标准）
3. assetFingerprint 强化（skeleton / morph / clip / key 全量语义）
4. OfflineFrameSequence 完整 capability 门控 + mirror 一致性
5. manifest backend = runtime.BackendName() + SessionIdentity 折入
6. Generic root motion 在 sequence v1 显式拒绝（ctor + RenderRange +
   Resume 三处）
7. Generic Resume 真验证（A/B/C 等价）
8. EGL display 引用计数（多 session 安全）
```

## 2. 代码改动

```text
src/runtime/wisteria_generic_runtime_driver.cpp
  StepMotionFrameExact：+config.loopMotion != frozen → DeterminismViolation
  RestoreCheckpoint：finite/playing/clipClamped/单位四元数校验；
    root bone 选择在 enabled=false 时保留
  ComputeAssetFingerprint：FingerprintBuilder 显式字节编码，
    覆盖 skeleton（name/parent/bind）、morph 定义全量、
    clip/track/key（translation/rotation/scale + 插值）、
    morph/IK 轨道
  IsDeterministicRootMotionEnabled()

include/wisteria/runtime/generic_checkpoint.hpp
  IDeterministicCheckpoint + IsDeterministicRootMotionEnabled()

src/scene/offline_frame_sequence.cpp
  capability 门控：exact + capture + restore + replay 全要求；
    mirror 与 deterministic 不一致 → logic_error
  root-motion 边界：ctor + RenderRange + Resume 拒绝
  manifest["backend"] = runtime->BackendName()
  SessionIdentity 折入 backend name

src/platform/egl_headless_context.cpp
  EGLDisplay 引用计数（std::mutex + unordered_map）；
    仅最后 owner eglTerminate

tests/headless_smoke.cpp
  sequence probe 重构为 A/B/C：
    A RenderRange(0..2) → B fresh Resume(4) → C from-start 0..4
    frame4 PNG 字节一致 + runtime pose 一致 + manifest backend 检查
    两个 live session 同时存在（依赖 display 引用计数）

tests/runtime_tests.cpp
  loopMotion 双向漂移
  in-memory restore 语义拒绝（NaN time/delta、playing=false、
    clipClamped 不一致、退化四元数）
  root bone 保留（enabled=false）
  fingerprint 区分 key 数据（同元数据不同动画 → 拒绝）
  sequence root-motion 边界（ctor + RenderRange 拒绝）
```

## 3. 验证结果（2026-08-09）

### 3.1 WSL Ubuntu 22.04（GCC，llvmpipe）

```text
headless-smoke：
  session probe PASS
  sequence probe PASS（RenderRange + fresh Resume + from-start 等价）
  （两个 live EGL session 并存，display 引用计数生效）
runtime：10/10 R1.8 PASS
unit / integration：全 PASS
```

### 3.2 Windows（MSVC RelWithDebInfo）

```text
runtime：10/10 R1.8 PASS
render-fbo PASS（R1.6 MMD 序列回归）
unit / integration：全 PASS
```

## 4. 当前状态

```text
R1.8 0A  CLOSED ✅
R1.8 0B  FINAL FIX APPLIED（待复审）
R1.8 0C  FINAL FIX APPLIED（待复审）
R1.8 0D  FINAL FIX APPLIED（待复审）
R1.8 0E  HOLD
```

