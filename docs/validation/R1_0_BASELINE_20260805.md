# R1.0 校正基线（2026-08-05）

> R1.0A 验收基线。本文件记录修正后的测试结果、工具链、fixture 哈希与
> 已知未覆盖项，是后续 R1.1 / R1.S 开发的工作树基准。

## 1. 代码基线

- R1.0 补丁提交：`783cfd9`（`p0`，2026-08-05，含 Direct PMX 断言修正）
- 分支：`wisteria2`
- R1.0A 工作树改动（未提交，本基线的一部分）：
  - `CMakeLists.txt`：新增 `WISTERIA_TEST_FULL_ASSETS` 档位开关与
    `WISTERIA_TEST_FIXTURES` manifest 路径、nlohmann include；
  - `tests/test_support.hpp`：新增 `NOT_CONFIGURED` 状态与
    `RequireFullAsset` / `RequireFullAssetDirectory`；
  - `tests/integration_tests.cpp`：fullAssets 检查档位化、修复
    `TestConvertedMmdGlbWhenAvailable` 错误路径（`仪玄` → `仪玄_glb`）；
  - `tests/fixtures/fixtures.json`：权威 fixture 清单（12 core +
    13 fullAssets）；
  - 归档文档：`R1_ENGINE_OWNERSHIP_ROADMAP.md`、
    `R1_0_CORRECTED_BASELINE.md`、`C_ABI_SAFETY_MATRIX.md`；
  - `.gitignore`：忽略 `build-fullassets/`。

## 2. 工具链

| 平台 | 编译器 | CMake | 配置 |
| ---- | ------ | ----- | ---- |
| Windows | MSVC（Visual Studio 17 2022） | 3.21.7 | RelWithDebInfo |
| Linux（WSL） | g++ 11.4.0 (Ubuntu 22.04) | 3.22.1 | 默认 + `LIBGL_ALWAYS_SOFTWARE=1` |

## 3. 测试结果（CORE 档，未启用 FULL_ASSETS）

### Windows

```text
CTest 4/4 通过
wisteria.integration：37 用例全部 [PASS]，0 SKIP / 0 NOT_CONFIGURED / 0 FAIL
```

### Linux（WSL + llvmpipe）

```text
CTest 4/4 通过
wisteria.integration：37 用例全部 [PASS]，0 SKIP / 0 NOT_CONFIGURED / 0 FAIL
```

### FULL_ASSETS 档位语义验证（Windows）

- `WISTERIA_TEST_FULL_ASSETS=ON` 且资产缺失 → `[FAIL] FULL_ASSETS fixture
  missing: production-pmx-yixuan`（硬失败，不允许 SKIP）；
- 默认档且资产缺失 → `[NOT_CONFIGURED] FULL_ASSETS fixture not configured`
  （不再伪装 PASS/SKIP）；
- 资产存在时两档均执行真实断言。

## 4. 修复记录

### Direct PMX MorphState 断言（R1.0 补丁自带冲突）

- 补丁前 HEAD 已存在断言 `morphInstance.HasMorphState()`；
- 补丁将 backend 驱动 PMX 的 Morph 所有权移入 Saba Runtime，断言冲突；
- 补丁验证环境缺 `仪玄.pmx`，用例 SKIP 掩盖冲突；
- 已改为验证 `ModelInstance` + `saba-mmd` runtime + 实例 Mesh +
  命名 Morph 经 runtime 控制；
- 双平台该用例均 `[PASS]`（真跑）。

### Converted MMD GLB 路径错误（R1.0A 发现）

- 测试引用 `tests/assets/models/仪玄/仪玄.glb`，实际目录为
  `仪玄_glb`，用例长期 SKIP；
- 已修正路径，修复后双平台 `[PASS]`（真实导入 21 mesh / 21 material /
  6 texture 断言通过）。

### Native ABI MMD control 静默 return（R1.0A 发现）

- 资产缺失时 `return;` 直接通过（绿灯陷阱）；
- 改为 `RequireFullAsset`，缺失时按档位 FAIL / NOT_CONFIGURED。

## 5. Fixture 哈希（SHA-256 前缀）

| Fixture | 哈希前缀 |
| ------- | -------- |
| `tests/fixtures/fixtures.json` | `50DEDCB1` |
| `tests/data/extended_morph.pmx` | `A936F734` |
| `assets/models/mmd/叶瞬光_pmx/叶瞬光.pmx` | `208E7484` |
| `assets/models/mmd/仪玄_pmx/仪玄.pmx` | `6314CDA9` |

## 6. 已知未覆盖项（后续阶段）

- GuardAbi 未覆盖导出函数（58 UNGUARDED + 15 raw-try，见
  `C_ABI_SAFETY_MATRIX.md`）→ R1.S；
- 中立 `ModelFrameSnapshot` 未实现 → R1.1；
- 静态/通用模型双路径 → R1.5；
- Context 显式资源根 → R1.3；
- 确定性 seek/replay → R1.2；
- 完整物理快照导出 → R1.2。

## 7. 复现命令

```bash
# Windows
cmake -S . -B build -DWISTERIA_BUILD_NATIVE=ON
cmake --build build --config RelWithDebInfo
ctest --test-dir build -C RelWithDebInfo

# Windows FULL_ASSETS 档位
cmake -S . -B build-fullassets -DWISTERIA_BUILD_NATIVE=ON -DWISTERIA_TEST_FULL_ASSETS=ON
cmake --build build-fullassets --config RelWithDebInfo
ctest --test-dir build-fullassets -C RelWithDebInfo

# Linux（WSL）
export LIBGL_ALWAYS_SOFTWARE=1
cmake --build build-linux
ctest --test-dir build-linux
```
