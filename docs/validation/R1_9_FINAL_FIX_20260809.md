# R1.9 Phase 0B–0D Final Fix 基线（2026-08-09）

> 状态：**COMPLETED（代码复审后）**；0E HOLD 至复审通过。

## 1. 修复清单（9 项）

```text
1. Windows UTF-8→UTF-16 越界写修复 + Unicode 路径测试
2. engine-owned ModelAssetBundle（ResourceManager + stable 共用），
   消除第二套资产 pipeline
3. capabilities：backend 身份来自 ModelAsset；Static 语义正确；
   mirror 一致性 fail-fast
4. RenderSession/GPU ownership：单 session/context + entity owner
   绑定 + GPU-safe teardown 顺序
5. GLFW 生命周期单一 refcount（Application + hidden provider）
6. Linux EGL 失败 → GLFW-hidden 真回退 + 强制失败钩子
7. stable render：size-query 先验 handle、sequence 异常同步、
   write 格式必选
8. ProbeCheckpointEnvelope（engine-owned，unknown kind → UNSUPPORTED）
9. 文档 KEEP 14→19、provider 描述修正
```

## 2. 代码改动

```text
src/native/windows_path.cpp           越界修复（wideLength / resize）
src/native/wisteria_stable_runtime.cpp
  capabilities 从 asset 反推 backend；Static 支持；
  mirror 检查；entity_destroy GPU-safe；Probe 分派
src/native/wisteria_stable_render.cpp
  size-query handle 前置；owner 绑定；session 单例；destroy 拒绝
  bound entity；sequence 异常同步；ValidateSequenceOptions 收紧
src/native/internal/stable_native_context.hpp
  StableEntityEntry ownerRenderSession；materials vector；
  StableContextState GPU-safe 析构（entities 先于 renderSessions）
src/assets/model_asset_bundle.hpp/.cpp（新增）
  ImportedModelData → ModelAsset + meshes + materials + textures
src/assets/manager.cpp
  LoadModel 改用 bundle（纹理缓存策略保留）
src/platform/glfw_lifetime.hpp（新增）+ application.cpp +
  egl_headless_context.cpp
  GLFW 生命周期统一；EGL→GLFW 回退；WISTERIA_HEADLESS_DISABLE_EGL
src/runtime/checkpoint_serialization.hpp/.cpp
  ProbeCheckpointEnvelope
src/scene/offline_frame_sequence.cpp
  AtomicReplace 失败附带 GetLastError（0D 遗留诊断）
CMakeLists.txt
  model_asset_bundle.cpp；headless-smoke-glfw-fallback ctest
tests/integration_tests.cpp
  +ownership lifecycle / static capabilities + unicode path /
   sequence failure state / GLFW lifetime shared with Application
docs/architecture/C_ABI_SAFETY_MATRIX.md（重新生成）
  94 legacy + 30 stable
```

## 3. 验证结果（2026-08-09）

```text
Windows CORE：9/9 PASS（integration 6/6 R1.9 用例）
Windows FULL：integration PASS（生产资产，R1.4/R1.9 stable 无回归）
Linux CORE（WSL，llvmpipe）：11/11 PASS
  （含 headless-smoke + headless-smoke-glfw-fallback）
```

## 4. 当前状态

```text
R1.9 0A  FROZEN ✅
R1.9 0B  FINAL FIX APPLIED（待复审）
R1.9 0C  APPROVED — CLOSED ✅
R1.9 0D  FINAL FIX APPLIED（待复审）
R1.9 0E  HOLD
```

